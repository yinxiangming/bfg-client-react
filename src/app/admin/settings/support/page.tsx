import SupportSettingsPage from '@/views/admin/settings/support/SupportSettingsPage'

export const metadata = { title: 'Settings - Support' }

export default function AdminSettingsSupportPage() {
  return <SupportSettingsPage />
}
